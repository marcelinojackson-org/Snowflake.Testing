name: Snowflake Suite CI

on:
  workflow_dispatch:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout orchestration repo
        uses: actions/checkout@v4

      - name: Checkout Snowflake.Common
        uses: actions/checkout@v4
        with:
          repository: marcelinojackson-org/Snowflake.Common
          path: Snowflake.Common
          token: ${{ secrets.SNOWFLAKE_GITHUB_PAT }}

      - name: Checkout Snowflake.ConnectGithubAction
        uses: actions/checkout@v4
        with:
          repository: marcelinojackson-org/Snowflake.ConnectGithubAction
          path: Snowflake.ConnectGithubAction
          token: ${{ secrets.SNOWFLAKE_GITHUB_PAT }}

      - name: Checkout Snowflake.RunSQLAction
        uses: actions/checkout@v4
        with:
          repository: marcelinojackson-org/Snowflake.RunSQLAction
          path: Snowflake.RunSQLAction
          token: ${{ secrets.SNOWFLAKE_GITHUB_PAT }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            Snowflake.Common/package-lock.json
            Snowflake.ConnectGithubAction/package-lock.json
            Snowflake.RunSQLAction/package-lock.json

      - name: Build Snowflake.Common
        working-directory: Snowflake.Common
        run: |
          npm ci
          npm test
          npm run build
          npm link

      - name: Build Snowflake.ConnectGithubAction
        working-directory: Snowflake.ConnectGithubAction
        run: |
          npm ci --ignore-scripts
          npm link @marcelinojackson-org/snowflake-common
          npm run build

      - name: Build Snowflake.RunSQLAction
        working-directory: Snowflake.RunSQLAction
        run: |
          npm ci --ignore-scripts
          npm link @marcelinojackson-org/snowflake-common
          npm run build

      - name: Smoke test connection
        working-directory: Snowflake.ConnectGithubAction
        env:
          SNOWFLAKE_ACCOUNT: ${{ vars.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_ACCOUNT_URL: ${{ vars.SNOWFLAKE_ACCOUNT_URL }}
          SNOWFLAKE_USER: ${{ vars.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD || secrets.SNOWFLAKE_PAT }}
          SNOWFLAKE_ROLE: ${{ vars.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ vars.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ vars.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_SCHEMA: ${{ vars.SNOWFLAKE_SCHEMA }}
          SNOWFLAKE_LOG_LEVEL: MINIMAL
        run: node dist/snowflake-connect.js

      - name: Run select current_schema
        working-directory: Snowflake.RunSQLAction
        env:
          RUN_SQL_STATEMENT: 'select current_schema() as current_schema'
          SNOWFLAKE_ACCOUNT: ${{ vars.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_ACCOUNT_URL: ${{ vars.SNOWFLAKE_ACCOUNT_URL }}
          SNOWFLAKE_USER: ${{ vars.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD || secrets.SNOWFLAKE_PAT }}
          SNOWFLAKE_ROLE: ${{ vars.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ vars.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ vars.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_SCHEMA: ${{ vars.SNOWFLAKE_SCHEMA }}
          SNOWFLAKE_LOG_LEVEL: MINIMAL
        run: node dist/snowflake-runsql.js

      - name: List tables for current schema
        working-directory: Snowflake.RunSQLAction
        env:
          RUN_SQL_STATEMENT: ${{ vars.SNOWFLAKE_TABLES_SQL || 'select * from information_schema.tables where table_schema = current_schema() order by table_name LIMIT 3' }}
          SNOWFLAKE_ACCOUNT: ${{ vars.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_ACCOUNT_URL: ${{ vars.SNOWFLAKE_ACCOUNT_URL }}
          SNOWFLAKE_USER: ${{ vars.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD || secrets.SNOWFLAKE_PAT }}
          SNOWFLAKE_ROLE: ${{ vars.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ vars.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ vars.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_SCHEMA: ${{ vars.SNOWFLAKE_SCHEMA }}
          SNOWFLAKE_LOG_LEVEL: MINIMAL
        run: node dist/snowflake-runsql.js
